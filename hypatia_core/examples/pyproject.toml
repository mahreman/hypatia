[project]
name = "hypatia"
version = "0.1.0"
description = "Hypatia: An intent-centric compiler for heterogeneous computing."
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"