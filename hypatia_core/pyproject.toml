[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hypatia-core"
version = "0.1.0"
description = "Hardware-Aware Symbolic Compiler for PyTorch"
requires-python = ">=3.8"
dependencies = []

[tool.maturin]
features = ["python"]
